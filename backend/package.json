{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "test": "pytest"
  }
}

